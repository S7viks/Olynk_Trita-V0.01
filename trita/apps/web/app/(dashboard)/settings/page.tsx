import { PlaceholderPage } from "@/components/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      phase="2"
      description="Tenant settings and notifications ship with inbox (RM-2)."
    />
  );
}
