import { PlaceholderPage } from "@/components/placeholder-page";

export default function InventoryPage() {
  return (
    <PlaceholderPage
      title="Inventory"
      phase="1"
      description="SKU list and cover metrics ship after six-app graph (RM-1)."
    />
  );
}
