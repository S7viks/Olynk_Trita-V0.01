[project]
name = "trita-ontology"
version = "0.0.1"
description = "Commerce graph and identity resolution"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/trita_ontology"]
