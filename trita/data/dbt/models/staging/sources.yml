version: 2

sources:
  - name: raw
    description: dlt raw envelope tables
    schema: raw
    tables:
      - name: shopify_events
        description: Shopify orders, inventory, products (JSON payload)
      - name: unicommerce_events
        description: Unicommerce inventory snapshots
      - name: shiprocket_events
        description: Shiprocket shipments
      - name: razorpay_events
        description: Razorpay settlements
      - name: csv_hub_events
        description: CSV hub canonical rows (tally, generic, …)
